# jenkins_container module

Starts a local Jenkins controller container with a persistent home volume and environment-specific ports.
Optionally seeds the Jenkins home volume from an existing Docker volume or host directory before startup.
