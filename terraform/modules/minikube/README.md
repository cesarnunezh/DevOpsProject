# minikube module

Manages a local Minikube profile through `local-exec` commands. It records the active profile metadata in a generated JSON file so root outputs can expose cluster connection details to CI/CD.
