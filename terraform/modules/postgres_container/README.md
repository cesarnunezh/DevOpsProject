# postgres_container module

Starts a local PostgreSQL container with persistent Docker volume storage and exposes it on an environment-specific host port.
Uses a published PostgreSQL image reference supplied by the root Terraform configuration.
