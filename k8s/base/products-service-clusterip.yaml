apiVersion: v1
kind: Service
metadata:
  name: products-api
spec:
  type: ClusterIP
  selector:
    app: products-api
    version: blue
  ports:
    - name: http
      port: 8070
      targetPort: http
