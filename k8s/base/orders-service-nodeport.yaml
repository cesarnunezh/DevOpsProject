apiVersion: v1
kind: Service
metadata:
  name: orders-api-nodeport
spec:
  type: NodePort
  selector:
    app: orders-api
    version: blue
  ports:
    - name: http
      port: 8050
      targetPort: http
      nodePort: 30050
