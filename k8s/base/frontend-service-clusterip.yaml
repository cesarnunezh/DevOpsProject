apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: ClusterIP
  selector:
    app: frontend-service
    version: blue
  ports:
    - name: http
      port: 80
      targetPort: http
