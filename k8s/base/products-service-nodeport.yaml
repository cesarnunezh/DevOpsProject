apiVersion: v1
kind: Service
metadata:
  name: products-api-nodeport
spec:
  type: NodePort
  selector:
    app: products-api
    version: blue
  ports:
    - name: http
      port: 8070
      targetPort: http
      nodePort: 30070
