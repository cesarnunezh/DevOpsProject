apiVersion: v1
kind: Service
metadata:
  name: frontend-service-nodeport
spec:
  type: NodePort
  selector:
    app: frontend-service
    version: blue
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
